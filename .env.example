# Copy to .env (gitignored) to override for a non-local build. Not needed for
# local dev against the docker-compose stack in server/ — buildsApi.js
# already defaults to it.

VITE_BUILDS_API_URL=http://localhost:8787
